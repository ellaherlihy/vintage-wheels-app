<div class="container mb-5">
  <br>
  <br>
    <center><a class="mt-5 btn btn-primary btn-lg" href=<%= new_car_path(@car) %>>Add a New Car</a></center>
    <div class="cards">
      <% current_user.cars.each do |car| %>
      <div class="card">
        <div class="card-category" style="background-image: linear-gradient(rgba(0,0,0,0.0), rgba(0,0,0,0.3)), url(<%=car.photo_url%>)">
        </div>
        <div class="card-body">
          <center><h5 class="card-title"><%= link_to "#{car.brand} #{car.model}", car_path(car)%></h5></center>
          <center><%= "£#{car.price.round} per day"%></center>
          <center><%=link_to "Edit details", edit_car_path(car), class: "btn btn-primary"%></center>
        </div>
      </div>
      <% end %>
    </div>
</div>
